<?xml version="1.0" encoding="utf-8"?>

<layout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable name="vm" type="com.whitecards.cadela.ui.workout.WorkoutViewModel"/>
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">


        <Button
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:id="@+id/workout_button" app:layout_constraintStart_toStartOf="parent" android:layout_marginStart="8dp"
                app:layout_constraintEnd_toEndOf="parent" android:layout_marginEnd="8dp" android:layout_marginBottom="8dp"
                app:layout_constraintBottom_toBottomOf="parent"
                android:onClick="@{(v) -> vm.onButtonClick(v)}"/>
        <ProgressBar
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:background="@drawable/circle_shape"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="8dp"
                android:indeterminateDrawable="@drawable/progress_circle"
                android:progressDrawable="@drawable/progress_circle"
                android:indeterminate="false"
                android:indeterminateOnly="false"
                android:id="@+id/timeProgressBar"
                android:min="0"
                android:max="10000"
                app:layout_constraintTop_toTopOf="parent" app:layout_constraintStart_toStartOf="parent"
                android:layout_marginStart="8dp" app:layout_constraintEnd_toEndOf="parent" android:layout_marginEnd="8dp"
                android:layout_marginBottom="8dp" app:layout_constraintBottom_toTopOf="@+id/guideline4"
                android:scrollbarSize="4dp">
        </ProgressBar>
        <androidx.constraintlayout.widget.Guideline
                android:orientation="horizontal"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" app:layout_constraintGuide_begin="306dp" android:id="@+id/guideline4"/>
        <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:id="@+id/workout_timer" app:layout_constraintStart_toStartOf="@+id/timeProgressBar"
                app:layout_constraintEnd_toEndOf="@+id/timeProgressBar" android:layout_marginEnd="8dp"
                app:layout_constraintTop_toTopOf="@+id/timeProgressBar"
                app:layout_constraintBottom_toBottomOf="@+id/timeProgressBar"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
